<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:background="@drawable/bg_round_white"
        app:layout_constraintWidth_percent="0.8"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:orientation="vertical"
        android:layout_width="0dp"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tv_pop_refuse_join_group_txt"
            android:padding="20dp"
            android:textSize="15dp"
            tools:text="您正在拒绝某人的入群申请"
            android:textColor="@color/titleBlack"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>


        <EditText
            android:id="@+id/et_pop_refuse_join_group_reason"
            android:textSize="13dp"
            android:paddingVertical="20dp"
            android:paddingHorizontal="8dp"
            android:gravity="top"
            android:hint="在此填写拒绝的理由（非必填）"
            android:layout_marginHorizontal="20dp"
            android:minLines="6"
            android:background="@drawable/bg_round_gray"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <TextView
            android:id="@+id/tv_pop_refuse_join_group_commit"
            android:paddingVertical="10dp"
            android:gravity="center"
            android:text="完成"
            android:textColor="@color/white"
            android:textSize="18dp"
            android:layout_margin="20dp"
            android:background="@drawable/bg_user_info_sex_cdts"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </LinearLayout>

</android.support.constraint.ConstraintLayout>